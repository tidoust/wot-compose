{
  "$schema": "http://json-schema.org/schema#",
  "title": "Web Thing Label Schema",
  "type": "object",
  "required": [
    "id",
    "name",
    "description",
    "createdAt",
    "tags",
    "customFields"
  ],
  "properties": {
    "id": {
    	"description": "https://tools.ietf.org/html/rfc3986#page-50",
      "type": "string", "pattern": "^(([^:/?#]+):)?(//([^/?#]*))?([^?#]*)(%5C?([^#]*))?(#(.*))?"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "customFields": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    }
  }
}